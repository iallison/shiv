<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>

  <div class="input-group-addon">
  <%= f.label :Role %><br />
  <%= f.radio_button :roles.to_s, 'root', :checked => true %> 
  <%= label :roles.to_s, 'root' %>
  <%= f.radio_button :roles.to_s, 'admin', :checked => false %> 
  <%= label :roles.to_s, 'admin' %>
  <%= f.radio_button :roles.to_s, 'guest', :checked => false %> 
  <%= label :roles.to_s, 'guest' %>
 
  </div>
  <br />

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

